{
  "System":{
    "Application":[
      {
        "name": "Audioplayer",
        "display": {
          "name": "Audio Player"
        },
        "package": "raxon/{{$this.name|>string.lowercase}}",
        "directory": {
          "test": "{{dd($this)}}",
          "application": "Application/{{$this.name}}/",
          "icon": "Application/{{$this.name}}/Icon/Icon.png"
        },
        "url": {
          "node_extension" : "{{config('project.dir.node')}}Data/System.Server.Extension.json",
          "node_content_type": "{{config('project.dir.node')}}Data/System.Server.ContentType.json",
          "extension": "{{config('project.dir.data')}}System.Server.Extension.json",
          "content_type": "{{config('project.dir.data')}}System.Server.ContentType.json"
        },
        "method": null,
        "target": null,
        "description": "Audio Player (Playing mp3, wav & ogg)",
        "user": "{{raw|>Package.Raxon.AudioPlayer:Setup:user.list(flags(), options(), $this)}}",
        "extension": "{{raw|>Package.Raxon.AudioPlayer:Setup:extension.list(flags(), options(), $this)}}"
      }
    ]
  }
}