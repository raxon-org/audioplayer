{
  "System":{
    "Application":[
      {
        "name": "Audioplayer",
        "user": "{{Package.Raxon.AudioPlayer:Setup:user.list($flags, $options)}}",
        "display": {
          "name": "Audio Player"
        },
        "directory": {
          "application": "Application/{{$this.name}}/",
          "icon": "Application/{{$this.name}}/Icon/Icon.png"
        },
        "method": null,
        "target": null,
        "description": "Audio Player (Playing mp3, wav & ogg)",
        "extension": "{{Package.Raxon.AudioPlayer:Setup:extension.list($flags, $options)}}"
      }
    ]
  }
}