{
  "System":{
    "Application":[
      {
        "name": "Audioplayer",
        "user": "{{raw|>Package.Raxon.AudioPlayer:Setup:user.list($flags, $options)}}",
        "display": {
          "name": "Audio Player"
        },
        "directory": {
          "application": "Application/{{$this.#parent.#parent.name}}/",
          "icon": "Application/{{$this.#parent.#parent.name}}/Icon/Icon.png"
        },
        "method": null,
        "target": null,
        "description": "Audio Player (Playing mp3, wav & ogg)",
        "extension": "{{raw|>Package.Raxon.AudioPlayer:Setup:extension.list($flags, $options)}}"
      }
    ]
  }
}